# YARP BFF sample

This folder will contain a YARP-based API Gateway and an example Backend-For-Frontend (BFF) that aggregates calls to multiple microservices and returns combined responses to clients.

Run instructions (placeholder):
1. Configure YARP routes in appsettings.json.
2. Provide service endpoints and any auth configuration via Key Vault or local settings.
3. Run the gateway project.